name: Cloud repair location
on: workflow_dispatch
permissions: {}
jobs:
  location:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Issue repairs run in the private photo-notes-repair-worker repository.'
